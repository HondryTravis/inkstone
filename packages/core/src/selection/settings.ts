
const wrapperClass = 'Selected'

export default {
    wrapperClass: wrapperClass,
    defaultSelectionWrapperOptions: {
        tag: 'span',
        props: {
            class: wrapperClass
        }
    }
}
